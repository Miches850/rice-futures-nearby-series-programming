# Outputs

The retained submitted Excel output is stored under:

`data/derived/Nearby_Series.xlsx`

After the final correction pass, a validated rerun should write a recreated output workbook to this folder and generate a corrected one-observation-per-trading-date nearby series.
