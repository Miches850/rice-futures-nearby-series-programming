# Derived Data

`Nearby_Series.xlsx` is the retained Excel export produced by the submitted SAS project.

It is preserved as a project artifact rather than presented as independently revalidated output.

During the final correction pass, compare this workbook against a correctly implemented version of the assignment's nearby-contract rule.
